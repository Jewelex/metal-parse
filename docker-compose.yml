version: "3.9"
 
services:
  app:
    build: .
    container_name: metal-parse
    restart: always
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./logs:/app/logs
      - ./scraper_output:/app/scraper_output
    shm_size: "512mb"
    networks:
      - web
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
 
networks:
  web:
    external: true